language: python
cache: pip
python:
  - "3.6"
install:
  - pip install -r requirements.txt
  - pip install coveralls
  - pip install codecov
script: xvfb-run coverage run -m unittest
after_success:
  - coveralls
  - codecov
